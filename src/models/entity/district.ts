import { BaseData } from '@maur025/core-model-data';

/**
 * @deprecated District is deprecated, use schema version
 */
export default interface District extends BaseData {
	name?: string;
	color?: string;
}
